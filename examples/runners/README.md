# Runner examples

This directory contains public runner examples used for local and hosted execution experiments.

- `web-to-dataset` — copied and adapted from the historical `riddle-runners` bundle
  with crawling/scraping/map task coverage and a `runner.json` manifest.
- `playwright-basic` — a lightweight Playwright screenshot runner skeleton with a
  stable `playwright_basic` task contract.
