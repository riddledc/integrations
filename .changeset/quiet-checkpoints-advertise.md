---
"@riddledc/riddle-proof": patch
---

Advertise all checkpoint response decisions that the runtime continuation path accepts, including recon `needs_recon` and generic `retry_stage`, and add formal/runtime conformance coverage for checkpoint response semantics.
