---
"@riddledc/riddle-proof": patch
---

Add setup return summary fields so `window_call` and `window_eval` profile
actions can promote selected returned JSON paths into compact `summary.md`
receipts.
