---
"@riddledc/riddle-proof-core": patch
"@riddledc/riddle-proof": patch
---

Add independently pinned rule and reusable evidence-template trust roots, bounded exact observation schemas, single-artifact capture identity enforcement, content-free review receipts separated from privileged packet bytes, deterministic replay, receipt chronology, and currentness verification, approved execution-policy binding, typed human attestations, and core-owned pure profile evaluators while keeping network and browser operations in the compatibility facade. The facade now keeps core external instead of rebundling a second implementation copy.
