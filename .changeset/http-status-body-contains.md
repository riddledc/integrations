---
"@riddledc/riddle-proof": patch
---

Add `body_contains` assertions to profile `http_status` checks so API profiles can verify response body content alongside status, content type, and byte evidence.
