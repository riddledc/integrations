---
"@riddledc/openclaw-riddle-proof": patch
---

Add `riddle_proof_wait` for detached proof monitoring and expose an explicit monitor plan so callers can avoid ad hoc sleep loops around status polling.
