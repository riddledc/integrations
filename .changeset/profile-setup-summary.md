---
"@riddledc/riddle-proof": patch
---

Add compact setup summaries to profile setup-action assessment evidence so setup-heavy clickthrough and iframe proofs expose per-viewport routes, action counts, clicked targets, frame URLs, text samples, and failures without requiring consumers to inspect every raw setup-action result.
