---
"@riddledc/riddle-proof": patch
---

Ignore setup actions scoped only to non-captured viewports when assessing profile runs, so viewport-filtered proof jobs do not fail on intentionally omitted viewport-specific actions.
