---
"@riddledc/riddle-proof": patch
---

Add opt-in `tap_burst_size` support for `tap_until` setup actions so cadence-sensitive gameplay proofs can dispatch several taps before the next predicate check while still recording compact tap and condition-check receipts.
