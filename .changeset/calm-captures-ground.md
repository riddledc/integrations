---
"@riddledc/riddle-proof": patch
---

Add signed grounded capture bundles, callback-free declarative JSON verifier and contract programs, deterministic observation verification, grounded Semantic receipts and certificates, replayed closure composition, and the accompanying Lean invariant model.

Add fixed content-addressed checked-meaning rules, trusted-rule replay, exact
root matching, and explicit consumption-time `checked`, `stale`, or
`unresolved` assessment, with a deliberately bounded Lean semantic model.
