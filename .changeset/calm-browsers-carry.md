---
"@riddledc/riddle-proof-runner-playwright": patch
---

Add a validated, runtime-only `extraHTTPHeaders` option for trusted Playwright targets without persisting header credentials into proof artifacts.
