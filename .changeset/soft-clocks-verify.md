---
"@riddledc/riddle-proof": patch
---

Expose the trusted `--strict true|false` option on the `riddle-run-script` CLI so proof scripts can opt into existing Riddle API strict-validation behavior from the command line.
