---
"@riddledc/openclaw-riddle-proof": patch
---

Normalize legacy OpenClaw plugin `defaultMaxIterations` values below 12 to the public proof-run minimum while preserving explicit per-run `max_iterations` overrides.
