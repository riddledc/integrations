---
"@riddledc/riddle-proof": patch
---

Add held-key support for profile `press` setup actions via `hold_ms` / `holdMs` so browser games that read key-down state can be proven with real keyboard input.
