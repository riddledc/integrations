---
"@riddledc/riddle-proof": patch
---

Add profile `target.network_mocks` so audit profiles can mock static backend responses, record mock hits in evidence, and fail required mocks that are not exercised.
