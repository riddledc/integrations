---
"@riddledc/riddle-proof": patch
---

Strengthen the handled list-load recovery example and docs around failed HTTP loads, explicit unavailable states, raw backend text absence, and avoiding brittle max-hit caps on idempotent GET mocks.
