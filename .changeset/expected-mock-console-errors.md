---
"@riddledc/riddle-proof": patch
---

Allow `no_fatal_console_errors` to treat browser resource-load console errors from expected failing network mocks as known-good evidence while still failing runtime errors and unmatched resource failures.
