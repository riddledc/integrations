---
"@riddledc/riddle-proof": patch
---

Add profile network mock hit caps with `max_hit_count` / `max_hits` and a `forbidden: true` shorthand so audit profiles can prove downstream requests did not run.
