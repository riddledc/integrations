---
"@riddledc/riddle-proof": patch
---

Expose `riddle-preview-deploy --framework spa|static` and a `deployPreview` client helper so SPA deep-link previews can use `pv_` fallback routing from the packaged CLI.
