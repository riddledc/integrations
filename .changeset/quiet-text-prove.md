---
"@riddledc/riddle-proof": patch
---

Include per-viewport text samples when whole-page text profile checks fail, so broad `text_visible` and `text_absent` assertions show the body text that made the check fail.
