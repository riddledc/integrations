---
"@riddledc/riddle-proof-core": patch
"@riddledc/riddle-proof": patch
---

Add independently pinned rule trust roots and reusable evidence-template trust roots, bounded exact observation schemas, single-artifact capture identity enforcement, nested checked-meaning composition, replay-derived evidence linkage, generic content-free receipts bound to private payload bytes, deterministic replay and chronology checks, approved execution-policy binding, and core-owned pure profile evaluators while keeping client semantics, network, and browser operations outside core. The compatibility facade now keeps core external instead of rebundling a second implementation copy.
