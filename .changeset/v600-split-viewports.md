---
"@riddledc/riddle-proof": patch
---

Add `run-profile --split-viewports` so multi-viewport profiles can run as one bounded Riddle job per viewport and aggregate the evidence into the original profile result.
