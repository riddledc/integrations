---
"@riddledc/riddle-proof": patch
---

Clarify that profile `http_status` body assertions match raw HTTP response bodies while `text_visible` and `selector_text_visible` check rendered browser text.
