---
"@riddledc/riddle-proof-packs": patch
---

Add explicit `cells`, `trackNames`, and `viewportNames` fields to Neon UI mixer control matrix summaries while keeping the legacy `viewports` rows for compatibility.
