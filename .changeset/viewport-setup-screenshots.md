---
"@riddledc/riddle-proof": patch
---

Allow profile setup screenshot actions to request viewport-only captures with `full_page: false`, `fullPage: false`, or `mode: "viewport"` so fixed and sticky page chrome does not pollute full-page milestone screenshots.
