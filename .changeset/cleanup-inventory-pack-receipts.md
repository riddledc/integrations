---
"@riddledc/riddle-proof": patch
---

Report proof-pack cleanup inventory receipts as failed when a failed setup `window_eval` or `window_call` receipt shows stale state remained after cleanup, instead of allowing broad text absence evidence to mark the receipt present.
