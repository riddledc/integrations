---
"@riddledc/riddle-proof": patch
---

Add `run-profile --result-format json|summary|none` so hosted profile runs can keep full JSON stdout for automation while agent loops can print a compact summary or suppress stdout when artifacts are already written.
