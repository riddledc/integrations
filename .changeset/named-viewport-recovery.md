---
"@riddledc/riddle-proof": patch
---

Add `run-profile --viewport-name` for named viewport selection, write immediate `riddle-job.json` receipts for hosted profile runs, and add `run-profile recover --job` to rebuild local profile artifacts from hosted Riddle job artifacts.
