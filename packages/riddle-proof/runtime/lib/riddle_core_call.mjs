#!/usr/bin/env node
// Compatibility launcher. The network-capable implementation is owned by the
// hosted client package so the facade does not carry a second endpoint stack.
import "@riddledc/riddle-proof-riddle-client/runtime/riddle-core-call";
