#!/usr/bin/env node
import("../dist/cli.js").then(
  ({ runInvoiceWorkbenchCli }) => runInvoiceWorkbenchCli(),
).catch((error) => {
  process.stderr.write(
    `${error instanceof Error ? error.message : "Invoice workbench failed."}\n`,
  );
  process.exitCode = 1;
});
